apiVersion: apps.sealos.io/v1
kind: App
metadata:
  name: location-tracking
  annotations:
    description: "Location Tracking Shortlink Service"
spec:
  CMD:
    - ./track
  args: []
  cpu: 0.5
  env:
    - name: PORT
      value: "8080"
    - name: DB_PATH
      value: /app/data/track.db
  image: your-dockerhub-username/location-tracking:latest
  memory: 512
  port: 8080
  replicas: 1
